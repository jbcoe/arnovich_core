
# CYGWIN build
BUILDLIB=/usr/lib/
BUILDBIN=$(TESTDIR)/bin/
CC=/usr/bin/g++
LN=/usr/bin/g++
CORE_CCARG= -x c -I include/ -I ./ -c -Wall
CORE_CCARGBIN= -x c -I include/ -I ./ -Wall -L$(BUILDLIB) -l$(LIBNAME)
LNARG=-Wl,-v -shared
EXTLIB=dll.a
PYLIB_EXT=

ifdef USE_PYTHON
	CORE_CCARG += -I /usr/include/python2.7/
	LNARG += -lpython2.7
endif

